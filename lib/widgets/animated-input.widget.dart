import 'package:flutter/material.dart';

class AnimatedInput extends StatelessWidget {
  const AnimatedInput({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
